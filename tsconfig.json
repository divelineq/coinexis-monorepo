{
  "extends": "./tsconfig.base.json",
  "compileOnSave": false, //? Отключает автоматическую сборку при сохранении файлов в IDE (VSCode)
  "files": [], //? Пустой список — этот tsconfig НЕ компилирует файлы напрямую, только управляет
  "references": [ //? Список всех проектов
    {
      "path": "./apps/client"
    },
    {
      "path": "./apps/server" 
    },
    {
      "path": "./packages/ui" 
    }
  ]
}
